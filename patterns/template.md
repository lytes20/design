# Template Design pattern.

You basically have a template of an algorithm.
The algorithm provides the structure or template. Different subclasses that implement this template will have some parts they do different. So they do those parts different
