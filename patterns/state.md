# State Pattern

The state pattern is a design pattern that allows objects to completely change it is behavior depending on its current internal state. - I guess React developers must know this one really well because they love state
